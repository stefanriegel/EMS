---
phase: 12-wizard-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/auth.py
  - tests/test_auth.py
  - ha-addon/run.sh
autonomous: true
requirements: [CFG-01, CFG-02]

must_haves:
  truths:
    - "EMS starts without loading ems_config.json — only env vars and Supervisor discovery"
    - "No setup wizard routes exist — /api/setup/* returns 404"
    - "Existing tests pass after setup_api.py, setup_config.py, test_setup_api.py, test_setup_config.py are deleted"
  artifacts:
    - path: "backend/main.py"
      provides: "Lifespan without setup_config layer"
      contains: "ensure_jwt_secret"
    - path: "tests/test_auth.py"
      provides: "Auth tests without setup_router dependency"
  key_links:
    - from: "backend/main.py"
      to: "backend/config.py"
      via: "HuaweiConfig.from_env() called directly without setup_config bootstrap"
      pattern: "HuaweiConfig\\.from_env"
---

<objective>
Remove backend setup wizard code and the ems_config.json config layer.

Purpose: CFG-01 requires setup wizard code removal; CFG-02 requires eliminating the ems_config.json persistence layer so Add-on options (env vars via run.sh) become the sole configuration surface.
Output: setup_api.py, setup_config.py, and their test files deleted; main.py lifespan simplified; test_auth.py updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py
@backend/setup_api.py
@backend/setup_config.py
@tests/test_auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete setup wizard backend files and clean main.py lifespan</name>
  <files>backend/setup_api.py, backend/setup_config.py, tests/test_setup_api.py, tests/test_setup_config.py, backend/main.py, ha-addon/run.sh</files>
  <read_first>backend/main.py, backend/setup_api.py, backend/setup_config.py, ha-addon/run.sh</read_first>
  <action>
1. Delete these files entirely:
   - `backend/setup_api.py`
   - `backend/setup_config.py`
   - `tests/test_setup_api.py`
   - `tests/test_setup_config.py`

2. Edit `backend/main.py` — remove the setup_config import and usage:
   - Remove the import line: `from backend.setup_config import load_setup_config, EMS_CONFIG_PATH`
   - Remove the import line: `from backend.setup_api import setup_router`
   - Remove the line: `app.include_router(setup_router)` from `create_app()`
   - In `create_app()`, replace `config_path = os.environ.get("EMS_CONFIG_PATH", EMS_CONFIG_PATH)` with `config_path = os.environ.get("EMS_CONFIG_PATH", "/config/ems_config.json")` — but since ems_config.json itself is being removed as a config layer, change the JWT secret directory resolution to use a simple default: `config_dir = os.environ.get("EMS_CONFIG_DIR", "/config")`. Actually, the JWT secret path just needs a directory. Use: `jwt_config_dir = os.path.dirname(os.environ.get("EMS_CONFIG_PATH", "/config/ems_config.json"))` to preserve backward compatibility with ha-addon/run.sh.

   In the lifespan function:
   - Remove the entire block (lines ~243-271) that loads wizard config and injects into env:
     ```
     config_path = os.environ.get("EMS_CONFIG_PATH", EMS_CONFIG_PATH)
     app.state.setup_config_path = config_path
     ...
     setup_cfg = load_setup_config(config_path)
     if setup_cfg is not None:
         os.environ.setdefault("HUAWEI_HOST", setup_cfg.huawei_host)
         ...
     ```
   - Keep the `ensure_jwt_secret(config_dir)` call but resolve `config_dir` from `os.environ.get("EMS_CONFIG_PATH", "/config/ems_config.json")` using `os.path.dirname()`. This preserves the JWT secret location without depending on setup_config module.
   - Remove `app.state.setup_config_path = config_path` entirely (no longer needed).
   - Update the KeyError except block (lines ~612-628): remove the comment referencing "open /setup to configure" and change the log message to say "missing required env var %s (check Add-on options or environment)" instead of "open /setup to configure".
   - Update the lifespan docstring: remove the sentence about "wizard config" and "setup-only mode" and "/setup". Replace with: "If required environment variables are absent, the lifespan starts in degraded mode: ``app.state.orchestrator`` is set to ``None`` and state endpoints return 503."

3. In `ha-addon/run.sh` — remove the line `export EMS_CONFIG_PATH="/config/ems_config.json"` since this path is no longer used as a config source. Keep the EMS_CONFIG_PATH export if the JWT secret still needs it — actually, keep it since `ensure_jwt_secret` in main.py still uses it for directory resolution. Leave line 126 (`export EMS_CONFIG_PATH="/config/ems_config.json"`) unchanged.

4. In `backend/auth.py` — no code changes needed (only a docstring mentions EMS_CONFIG_PATH, which is still a valid env var for JWT secret path resolution).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/ems && python -c "from backend.main import create_app; print('import ok')" && ! test -f backend/setup_api.py && ! test -f backend/setup_config.py && ! test -f tests/test_setup_api.py && ! test -f tests/test_setup_config.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/setup_api.py` does not exist
    - `backend/setup_config.py` does not exist
    - `tests/test_setup_api.py` does not exist
    - `tests/test_setup_config.py` does not exist
    - `backend/main.py` does NOT contain `from backend.setup_config import`
    - `backend/main.py` does NOT contain `from backend.setup_api import`
    - `backend/main.py` does NOT contain `setup_router`
    - `backend/main.py` does NOT contain `load_setup_config`
    - `backend/main.py` does NOT contain `app.state.setup_config_path`
    - `backend/main.py` DOES contain `ensure_jwt_secret`
    - `backend/main.py` lifespan KeyError log message contains "Add-on options" not "/setup"
    - `python -c "from backend.main import create_app"` exits 0
  </acceptance_criteria>
  <done>setup_api.py, setup_config.py, and both test files deleted. main.py lifespan no longer loads ems_config.json. JWT secret resolution preserved. No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_auth.py to remove setup_router dependency</name>
  <files>tests/test_auth.py</files>
  <read_first>tests/test_auth.py</read_first>
  <action>
In `tests/test_auth.py`, find the `_make_test_app` helper function (around line 88-119):

1. Remove the import: `from backend.setup_api import setup_router`
2. Remove the line: `app.include_router(setup_router)`
3. Remove the line: `app.state.setup_config_path = "/tmp/ems-test-config.json"` and its comment

These are the only three references. The rest of test_auth.py tests auth endpoints that are independent of the setup wizard.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/ems && python -m pytest tests/test_auth.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_auth.py` does NOT contain `setup_router`
    - `tests/test_auth.py` does NOT contain `setup_config_path`
    - `tests/test_auth.py` does NOT contain `from backend.setup_api`
    - `python -m pytest tests/test_auth.py -x` exits 0
  </acceptance_criteria>
  <done>test_auth.py no longer references setup wizard code and all auth tests pass.</done>
</task>

</tasks>

<verification>
```bash
# 1. Verify deleted files are gone
! test -f backend/setup_api.py && ! test -f backend/setup_config.py && echo "wizard files deleted"

# 2. Verify main.py imports cleanly
python -c "from backend.main import create_app; print('ok')"

# 3. Verify no setup_router references remain in production code
! grep -r "setup_router" backend/ && echo "no setup_router refs in backend"

# 4. Verify no setup_config references remain in production code
! grep -r "from backend.setup_config" backend/ && echo "no setup_config refs in backend"

# 5. Run remaining tests
python -m pytest tests/test_auth.py -x -q
```
</verification>

<success_criteria>
- setup_api.py, setup_config.py, and their test files are deleted (CFG-01)
- main.py lifespan reads config exclusively from env vars and Supervisor discovery — no ems_config.json fallback (CFG-02)
- All remaining tests pass
- JWT secret generation path is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/12-wizard-removal/12-01-SUMMARY.md`
</output>
