---
phase: 13-mqtt-discovery-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ha-addon/translations/en.yaml
autonomous: true
requirements:
  - DISC-13

must_haves:
  truths:
    - "Every option in ha-addon/config.yaml has a corresponding entry in translations/en.yaml with name and description"
    - "No raw key names are shown in the HA add-on config page"
  artifacts:
    - path: "ha-addon/translations/en.yaml"
      provides: "Human-readable names and descriptions for all config options"
      contains: "configuration:"
  key_links:
    - from: "ha-addon/translations/en.yaml"
      to: "ha-addon/config.yaml"
      via: "Key names in translations must match option keys in config.yaml"
      pattern: "huawei_host"
---

<objective>
Audit and complete the Add-on translations file to cover all config options.

Purpose: Ensure every config option in config.yaml has a human-readable name and description in en.yaml so the HA add-on config page shows clean labels instead of raw key names.

Output: Complete ha-addon/translations/en.yaml with all config options covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ha-addon/config.yaml
@ha-addon/translations/en.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete en.yaml translations for all config options</name>
  <files>ha-addon/translations/en.yaml</files>
  <read_first>ha-addon/config.yaml, ha-addon/translations/en.yaml</read_first>
  <action>
1. **Extract all option keys from config.yaml** `options:` section. The current keys are:
   huawei_host, huawei_port, huawei_master_unit_id, huawei_slave_unit_id,
   victron_host, victron_port, victron_system_unit_id, victron_battery_unit_id, victron_vebus_unit_id,
   influxdb_url, influxdb_token, influxdb_org, influxdb_bucket,
   huawei_deadband_w, victron_deadband_w, ramp_rate_w_per_cycle, min_soc_pct_huawei, min_soc_pct_victron,
   ha_db_path, ha_statistics_entity_outdoor_temp, ha_statistics_entity_heat_pump, ha_statistics_entity_dhw, ha_ml_min_days,
   ha_octopus_entity_id,
   modul3_surplus_start_min, modul3_surplus_end_min, modul3_deficit_start_min, modul3_deficit_end_min,
   modul3_surplus_rate_eur_kwh, modul3_deficit_rate_eur_kwh,
   feed_in_rate_eur_kwh,
   winter_months, winter_min_soc_boost_pct,
   log_level

2. **Also check schema: section** for additional keys that appear only in schema (not in options defaults). These are:
   evcc_host, evcc_port, ha_heat_pump_entity_id, telegram_bot_token, telegram_chat_id, admin_password_hash

3. **Compare against existing en.yaml** -- the current en.yaml already has translations for ALL the above keys. Verify completeness by diffing the key lists.

4. **If any keys are missing**, add them with clear name and description following the established pattern:
   ```yaml
   key_name:
     name: Human Readable Name
     description: >-
       Clear description of what this option does and what value to provide.
   ```

5. **Quality check the existing descriptions** -- ensure they are accurate given the v1.2 changes (e.g., wizard removal means no wizard references in descriptions). Fix any outdated references.

6. **Verify the YAML is valid** by checking indentation and structure. The file must start with `configuration:` and every option must have both `name:` and `description:` sub-keys.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/ems && python3 -c "
import yaml
with open('ha-addon/config.yaml') as f:
    config = yaml.safe_load(f)
with open('ha-addon/translations/en.yaml') as f:
    trans = yaml.safe_load(f)
options = set(config.get('options', {}).keys())
schema = set(config.get('schema', {}).keys())
all_keys = options | schema
trans_keys = set(trans.get('configuration', {}).keys())
missing = all_keys - trans_keys
extra = trans_keys - all_keys
print(f'Config keys: {len(all_keys)}')
print(f'Translation keys: {len(trans_keys)}')
if missing: print(f'MISSING translations: {missing}')
else: print('All keys have translations')
if extra: print(f'Extra translation keys (ok): {extra}')
for key in trans_keys:
    entry = trans['configuration'][key]
    assert 'name' in entry, f'{key} missing name'
    assert 'description' in entry, f'{key} missing description'
print('All entries have name and description')
"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import yaml; t=yaml.safe_load(open('ha-addon/translations/en.yaml')); print(len(t['configuration']))" outputs a number >= 36 (all config+schema keys)
    - The verify script prints "All keys have translations" and "All entries have name and description"
    - grep -q "configuration:" ha-addon/translations/en.yaml
    - No YAML syntax errors when loading the file
  </acceptance_criteria>
  <done>
    Every config.yaml option key has a corresponding en.yaml translation entry with name and description. No raw key names shown in HA add-on config page.
  </done>
</task>

</tasks>

<verification>
- `python3 -c "import yaml; ..."` verify script from above passes
- Manual check: count of translation keys >= count of config option + schema keys
</verification>

<success_criteria>
- en.yaml covers 100% of config.yaml options and schema keys
- Every entry has both name and description
- YAML is valid and parseable
- No outdated references to setup wizard
</success_criteria>

<output>
After completion, create `.planning/phases/13-mqtt-discovery-overhaul/13-03-SUMMARY.md`
</output>
