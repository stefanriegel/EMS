---
phase: 23-production-commissioning
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - backend/coordinator.py
  - backend/victron_controller.py
  - backend/api.py
  - backend/main.py
  - tests/test_victron_watchdog_guard.py
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03

must_haves:
  truths:
    - "Coordinator checks commissioning gate before executing any hardware write"
    - "Shadow mode logs decisions with trigger=shadow_mode and suppresses all execute calls"
    - "All 8 execute() call sites in _run_cycle() route through _execute_commands()"
    - "Victron 45s guard writes 0W to all 3 phases as independent background task"
    - "Victron 45s guard skips writes during validation period"
    - "/api/health returns commissioning stage, shadow mode, and progression status"
    - "CommissioningManager is wired in main.py lifespan and exposed on app.state"
  artifacts:
    - path: "backend/coordinator.py"
      provides: "_execute_commands() method with shadow mode and stage gating"
      contains: "_execute_commands"
    - path: "backend/victron_controller.py"
      provides: "start_watchdog_guard() background task"
      contains: "watchdog_guard"
    - path: "backend/api.py"
      provides: "Commissioning section in /api/health"
      contains: "commissioning"
    - path: "backend/main.py"
      provides: "CommissioningManager lifespan wiring"
      contains: "CommissioningManager"
    - path: "tests/test_victron_watchdog_guard.py"
      provides: "Watchdog guard unit tests"
      min_lines: 40
  key_links:
    - from: "backend/coordinator.py"
      to: "backend/commissioning.py"
      via: "set_commissioning_manager() injection"
      pattern: "set_commissioning_manager"
    - from: "backend/coordinator.py"
      to: "backend/controller_model.py"
      via: "DecisionEntry with trigger=shadow_mode"
      pattern: "trigger.*shadow_mode"
    - from: "backend/main.py"
      to: "backend/commissioning.py"
      via: "CommissioningManager instantiation in lifespan"
      pattern: "CommissioningManager"
    - from: "backend/victron_controller.py"
      to: "VictronDriver.write_ac_power_setpoint"
      via: "45s guard zero-write"
      pattern: "write_ac_power_setpoint.*0"
---

<objective>
Wire CommissioningManager into the coordinator (shadow mode + stage gating), add Victron 45s watchdog guard, extend /api/health, and connect everything in main.py lifespan.

Purpose: Activates the commissioning safety net so that production deployment follows staged rollout with shadow mode and watchdog protection.
Output: Updated coordinator.py, victron_controller.py, api.py, main.py, and tests/test_victron_watchdog_guard.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-production-commissioning/23-RESEARCH.md
@.planning/phases/23-production-commissioning/23-01-SUMMARY.md

@backend/coordinator.py
@backend/victron_controller.py
@backend/api.py
@backend/main.py
@backend/commissioning.py
</context>

<interfaces>
<!-- From Plan 01 output (backend/commissioning.py) -->
```python
class CommissioningStage(str, enum.Enum):
    READ_ONLY = "READ_ONLY"
    SINGLE_BATTERY = "SINGLE_BATTERY"
    DUAL_BATTERY = "DUAL_BATTERY"

class CommissioningState:
    stage: CommissioningStage
    shadow_mode: bool
    stage_entered_at: float
    read_only_min_hours: float
    single_battery_min_hours: float
    def can_write_victron(self) -> bool: ...
    def can_write_huawei(self) -> bool: ...

class CommissioningManager:
    def __init__(self, config: CommissioningConfig): ...
    def load_or_init(self) -> None: ...
    def advance(self) -> bool: ...
    @property
    def stage(self) -> CommissioningStage: ...
    @property
    def shadow_mode(self) -> bool: ...
    @property
    def state(self) -> CommissioningState: ...
    def get_progression_status(self) -> dict: ...
    @property
    def stage_entered_at_iso(self) -> str | None: ...
```

<!-- From backend/coordinator.py (lines with execute calls) -->
Lines 603-604, 645-646, 658-659, 670-671, 706-707, 739-740, 754-755, 809-810:
All follow pattern:
```python
await self._huawei_ctrl.execute(h_cmd)
await self._victron_ctrl.execute(v_cmd)
```

<!-- From backend/controller_model.py -->
```python
class DecisionEntry:
    timestamp: str
    trigger: str  # "shadow_mode" for shadow decisions
    huawei_role: str
    victron_role: str
    p_target_w: float
    huawei_allocation_w: float
    victron_allocation_w: float
    pool_status: str
    reasoning: str
```

<!-- From backend/victron_controller.py -->
```python
class VictronController:
    def _in_validation_period(self) -> bool: ...
    # _driver.write_ac_power_setpoint(phase: int, watts: float, dry_run: bool = False) -> None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coordinator _execute_commands() with shadow mode and stage gating</name>
  <files>backend/coordinator.py, backend/controller_model.py</files>
  <read_first>backend/coordinator.py, backend/commissioning.py, backend/controller_model.py</read_first>
  <behavior>
    - test_shadow_mode_logs_decision_no_execute: when commissioning_manager.shadow_mode is True, _execute_commands logs DecisionEntry with trigger="shadow_mode" and does NOT call controller.execute()
    - test_stage_gates_victron_write: in READ_ONLY stage, victron execute() is NOT called; in SINGLE_BATTERY stage, it IS called
    - test_stage_gates_huawei_write: in READ_ONLY and SINGLE_BATTERY stages, huawei execute() is NOT called; in DUAL_BATTERY stage, it IS called
    - test_no_commissioning_manager_allows_all: when commissioning_manager is None, both controllers execute normally (backward compat)
    - test_all_execute_sites_use_central_method: grep coordinator.py for direct execute() calls — only _execute_commands and safe-state writes should remain
  </behavior>
  <action>
    1. Add set_commissioning_manager(self, manager) method to Coordinator (follows existing set_xxx pattern, stores as self._commissioning_manager, init to None in __init__).

    2. Add _execute_commands(self, h_cmd: ControllerCommand, v_cmd: ControllerCommand) async method:
       - If commissioning manager exists and shadow_mode is True: append DecisionEntry with trigger="shadow_mode", reasoning="Shadow mode: decisions logged, writes suppressed", then return without calling execute()
       - If commissioning manager exists: check can_write_huawei() before huawei execute, check can_write_victron() before victron execute. Log debug when blocked.
       - If commissioning manager is None: execute both (backward compat)

    3. Replace ALL 8 direct execute() call pairs in _run_cycle() (lines ~603-604, 645-646, 658-659, 670-671, 706-707, 739-740, 754-755, 809-810) with calls to await self._execute_commands(h_cmd, v_cmd). Do NOT touch safe-state writes (those use zero setpoint on comm failure and must bypass commissioning).

    4. In _build_state(), populate the commissioning_stage and commissioning_shadow_mode fields from the commissioning manager if present.

    IMPORTANT: Safe-state writes (the consecutive failure path that writes zero) must NOT go through _execute_commands — they are emergency writes that bypass all gating. Verify by checking that the safe-state code path still calls controller.execute() directly.

    After refactoring, grep for "\.execute(" in coordinator.py should show: _execute_commands method body (2 calls), safe-state write paths, and NO other direct execute calls in _run_cycle().
  </action>
  <verify>
    <automated>python -m pytest tests/test_commissioning.py tests/test_coordinator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "set_commissioning_manager" backend/coordinator.py
    - grep -q "_execute_commands" backend/coordinator.py
    - grep -q "shadow_mode" backend/coordinator.py
    - grep -q "can_write_huawei" backend/coordinator.py
    - grep -q "can_write_victron" backend/coordinator.py
    - python -m pytest tests/test_coordinator.py -x -q exits 0
  </acceptance_criteria>
  <done>All 8 execute call sites in _run_cycle() route through _execute_commands(). Shadow mode logs decisions without executing. Stage gating blocks writes per commissioning stage. Safe-state writes still bypass commissioning. Existing coordinator tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Victron 45s watchdog guard + API health + lifespan wiring</name>
  <files>backend/victron_controller.py, backend/api.py, backend/main.py, tests/test_victron_watchdog_guard.py</files>
  <read_first>backend/victron_controller.py, backend/api.py, backend/main.py</read_first>
  <behavior>
    - test_guard_fires_zero_write: after 45s sleep, guard writes 0W to phases 1, 2, 3 via write_ac_power_setpoint
    - test_guard_skips_during_validation: when _in_validation_period() returns True, guard does not call write_ac_power_setpoint
    - test_guard_handles_write_failure: when write_ac_power_setpoint raises, guard logs warning and continues loop
    - test_guard_cancellation: guard task can be cancelled cleanly
  </behavior>
  <action>
    1. Add to VictronController:
       - _watchdog_guard_task: asyncio.Task | None = None (in __init__)
       - start_watchdog_guard() -> None: creates asyncio.create_task(self._watchdog_guard_loop()), stores in _watchdog_guard_task. No-op if already running.
       - stop_watchdog_guard() -> None: cancels _watchdog_guard_task, awaits it with CancelledError catch.
       - _watchdog_guard_loop() -> None: async infinite loop — await asyncio.sleep(45), check _in_validation_period(), if not in validation: write 0W to phases 1,2,3 via self._driver.write_ac_power_setpoint(phase, 0.0). Wrap writes in try/except, log WARNING on failure. Log debug on success. If in validation period, log debug "skipped — validation period active".

    2. Add commissioning section to /api/health in backend/api.py:
       - Read commissioning_manager from request.app.state (getattr with None default)
       - If present, add "commissioning" dict to health response: stage, shadow_mode, stage_entered_at (ISO), progression_criteria (from get_progression_status())
       - If None, omit the commissioning key (backward compat)

    3. Wire in backend/main.py lifespan:
       - Import CommissioningConfig and CommissioningManager from backend.config and backend.commissioning
       - After coordinator creation, before coordinator.start():
         a. commissioning_cfg = CommissioningConfig.from_env()
         b. If commissioning_cfg.enabled: create CommissioningManager(commissioning_cfg), call load_or_init(), wire via coordinator.set_commissioning_manager(), store on app.state.commissioning_manager
         c. Log commissioning stage and shadow mode
       - After victron controller creation: call victron_ctrl.start_watchdog_guard() (the guard self-checks validation period, so safe to start always)
       - In shutdown (after yield): call victron_ctrl.stop_watchdog_guard() before driver disconnect

    4. Create tests/test_victron_watchdog_guard.py with all behaviors. Mock the driver's write_ac_power_setpoint. Use monkeypatch for _in_validation_period. Use anyio for async tests.

    Follow conventions: from __future__ import annotations, type hints, logger = logging.getLogger(__name__), fire-and-forget for guard failures.
  </action>
  <verify>
    <automated>python -m pytest tests/test_victron_watchdog_guard.py tests/test_commissioning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "watchdog_guard" backend/victron_controller.py
    - grep -q "start_watchdog_guard" backend/victron_controller.py
    - grep -q "stop_watchdog_guard" backend/victron_controller.py
    - grep -q "commissioning" backend/api.py
    - grep -q "CommissioningManager" backend/main.py
    - grep -q "CommissioningConfig" backend/main.py
    - grep -q "start_watchdog_guard" backend/main.py
    - python -m pytest tests/test_victron_watchdog_guard.py -x -q exits 0
    - python -m pytest tests/ -q exits 0
  </acceptance_criteria>
  <done>Victron 45s watchdog guard runs as independent background task, skips during validation. /api/health shows commissioning state. CommissioningManager wired in lifespan. All tests pass including full suite.</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_commissioning.py tests/test_victron_watchdog_guard.py -x -q
python -m pytest tests/ -q
# Verify no direct execute() calls leaked in _run_cycle:
grep -n "\.execute(" backend/coordinator.py | grep -v "_execute_commands" | grep -v "safe.state\|consecutive_fail"
</verification>

<success_criteria>
- All 8 execute() call sites in _run_cycle() route through _execute_commands()
- Shadow mode logs DecisionEntry with trigger="shadow_mode" and suppresses all writes
- Stage gating blocks Huawei writes until DUAL_BATTERY, Victron writes until SINGLE_BATTERY
- Safe-state writes bypass commissioning entirely
- Victron 45s guard runs independently, writes 0W to 3 phases, skips during validation
- /api/health returns commissioning stage, shadow mode, progression status
- CommissioningManager loads persisted state on startup
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/23-production-commissioning/23-02-SUMMARY.md`
</output>
