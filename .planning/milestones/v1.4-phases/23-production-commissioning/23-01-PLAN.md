---
phase: 23-production-commissioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/commissioning.py
  - backend/config.py
  - backend/controller_model.py
  - tests/test_commissioning.py
autonomous: true
requirements:
  - PROD-01
  - PROD-02

must_haves:
  truths:
    - "CommissioningManager progresses through READ_ONLY -> SINGLE_BATTERY -> DUAL_BATTERY stages"
    - "Stage transition is blocked when minimum time criteria are not met"
    - "Shadow mode flag controls whether writes are suppressed"
    - "Commissioning state persists across restarts via JSON file"
    - "can_write_victron() returns True only in SINGLE_BATTERY or DUAL_BATTERY when not in shadow mode"
    - "can_write_huawei() returns True only in DUAL_BATTERY when not in shadow mode"
  artifacts:
    - path: "backend/commissioning.py"
      provides: "CommissioningManager state machine with stage progression, persistence, shadow mode"
      exports: ["CommissioningStage", "CommissioningState", "CommissioningManager"]
    - path: "backend/config.py"
      provides: "CommissioningConfig dataclass with from_env()"
      contains: "class CommissioningConfig"
    - path: "backend/controller_model.py"
      provides: "Commissioning fields on CoordinatorState"
      contains: "commissioning_stage"
    - path: "tests/test_commissioning.py"
      provides: "Unit tests for state machine, persistence, shadow mode"
      min_lines: 80
  key_links:
    - from: "backend/commissioning.py"
      to: "backend/config.py"
      via: "CommissioningConfig import"
      pattern: "from backend\\.config import CommissioningConfig"
    - from: "backend/commissioning.py"
      to: "/config/ems_commissioning.json"
      via: "JSON file persistence"
      pattern: "json\\.dump|json\\.load"
---

<objective>
Create the CommissioningManager state machine, CommissioningConfig, CoordinatorState extensions, and comprehensive tests.

Purpose: Provides the gating logic that controls which batteries can receive writes at each commissioning stage, plus shadow mode for logging-only operation.
Output: backend/commissioning.py, updated backend/config.py, updated backend/controller_model.py, tests/test_commissioning.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-production-commissioning/23-RESEARCH.md

@backend/config.py
@backend/controller_model.py
</context>

<interfaces>
<!-- Key types the executor needs from existing code -->

From backend/config.py (pattern to follow for CommissioningConfig):
```python
@dataclass
class HardwareValidationConfig:
    dry_run: bool = False
    validation_period_hours: float = 48.0

    @classmethod
    def from_env(cls) -> "HardwareValidationConfig":
        return cls(
            dry_run=os.environ.get("EMS_DRY_RUN", "false").lower() == "true",
            validation_period_hours=float(os.environ.get("EMS_VALIDATION_PERIOD_HOURS", "48")),
        )
```

From backend/controller_model.py:
```python
class BatteryRole(str, Enum):
    ...

class CoordinatorState:
    # Last fields currently:
    huawei_working_mode: str = "unknown"

class DecisionEntry:
    timestamp: str
    trigger: str
    huawei_role: str
    victron_role: str
    p_target_w: float
    huawei_allocation_w: float
    victron_allocation_w: float
    pool_status: str
    reasoning: str
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CommissioningManager module + CommissioningConfig + tests</name>
  <files>backend/commissioning.py, backend/config.py, backend/controller_model.py, tests/test_commissioning.py</files>
  <read_first>backend/config.py, backend/controller_model.py, backend/huawei_mode_manager.py</read_first>
  <behavior>
    - test_initial_stage_is_read_only: new manager with no persisted state starts at READ_ONLY
    - test_stage_progression_read_only_to_single: advance() moves from READ_ONLY to SINGLE_BATTERY after min hours met
    - test_stage_progression_single_to_dual: advance() moves from SINGLE_BATTERY to DUAL_BATTERY after min hours met
    - test_advance_blocked_when_criteria_not_met: advance() returns False and stays at current stage when time criteria not met
    - test_no_advance_past_dual: advance() at DUAL_BATTERY is a no-op
    - test_can_write_victron_per_stage: READ_ONLY=False, SINGLE_BATTERY=True, DUAL_BATTERY=True (all with shadow_mode=False)
    - test_can_write_huawei_per_stage: READ_ONLY=False, SINGLE_BATTERY=False, DUAL_BATTERY=True (all with shadow_mode=False)
    - test_shadow_mode_blocks_all_writes: can_write_victron()=False and can_write_huawei()=False when shadow_mode=True regardless of stage
    - test_state_persistence_save_load: save state to tmp file, create new manager, load_or_init() restores same stage and shadow_mode
    - test_state_persistence_missing_file: load_or_init() with no file starts at READ_ONLY
    - test_config_from_env: CommissioningConfig.from_env() reads all expected env vars with correct defaults
    - test_get_progression_status: returns dict with time_in_stage_hours, min_hours_required, can_advance
  </behavior>
  <action>
    1. Create backend/commissioning.py with:
       - CommissioningStage(str, enum.Enum): READ_ONLY, SINGLE_BATTERY, DUAL_BATTERY
       - CommissioningState dataclass: stage, shadow_mode, stage_entered_at (float, time.time()), read_only_min_hours, single_battery_min_hours. Methods: can_write_victron() -> bool, can_write_huawei() -> bool (logic from research Pattern 1)
       - CommissioningManager class:
         - __init__(self, config: CommissioningConfig)
         - load_or_init() -> None: loads state from JSON file or creates default READ_ONLY state
         - advance() -> bool: checks time-in-stage criteria, advances to next stage, saves, returns True if advanced
         - property stage -> CommissioningStage
         - property shadow_mode -> bool
         - property state -> CommissioningState
         - get_progression_status() -> dict: returns time_in_stage_hours, min_hours_required, can_advance
         - stage_entered_at_iso property for API display
         - _save_state() / _load_state() using atomic os.replace() with .tmp file (from research Pattern 4)

    2. Add CommissioningConfig to backend/config.py (after ModeManagerConfig):
       - enabled: bool = True (EMS_COMMISSIONING_ENABLED, default "true")
       - shadow_mode: bool = True (EMS_SHADOW_MODE, default "true")
       - state_file_path: str = "/config/ems_commissioning.json" (EMS_COMMISSIONING_STATE_PATH)
       - read_only_min_hours: float = 24.0 (EMS_READ_ONLY_MIN_HOURS)
       - single_battery_min_hours: float = 24.0 (EMS_SINGLE_BATTERY_MIN_HOURS)
       - @classmethod from_env() following existing pattern

    3. Add commissioning fields to CoordinatorState in backend/controller_model.py:
       - commissioning_stage: str = "DUAL_BATTERY" (default = fully operational for backward compat)
       - commissioning_shadow_mode: bool = False

    4. Create tests/test_commissioning.py with all behaviors listed above. Use tmp_path fixture for state file. Use monkeypatch for env vars and time.time().

    Follow existing conventions: from __future__ import annotations, type hints, logger = logging.getLogger(__name__), snake_case, 4-space indent.
  </action>
  <verify>
    <automated>python -m pytest tests/test_commissioning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class CommissioningStage" backend/commissioning.py
    - grep -q "class CommissioningManager" backend/commissioning.py
    - grep -q "class CommissioningConfig" backend/config.py
    - grep -q "commissioning_stage" backend/controller_model.py
    - grep -q "commissioning_shadow_mode" backend/controller_model.py
    - grep -q "def test_stage_progression" tests/test_commissioning.py
    - grep -q "def test_shadow_mode_blocks" tests/test_commissioning.py
    - grep -q "def test_state_persistence" tests/test_commissioning.py
    - python -m pytest tests/test_commissioning.py -x -q exits 0
  </acceptance_criteria>
  <done>CommissioningManager with stage progression, shadow mode, JSON persistence, and CommissioningConfig all pass unit tests. CoordinatorState has commissioning fields.</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_commissioning.py -x -q
python -m pytest tests/ -q  # full suite still green
</verification>

<success_criteria>
- CommissioningStage enum with 3 stages exists
- CommissioningManager handles stage progression with time-based criteria
- Shadow mode blocks all writes regardless of stage
- State persists to JSON and restores on init
- CommissioningConfig reads all 5 env vars with sensible defaults
- CoordinatorState extended with commissioning_stage and commissioning_shadow_mode
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-production-commissioning/23-01-SUMMARY.md`
</output>
