---
phase: 24-vrm-dess-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/dess_models.py
  - backend/vrm_client.py
  - backend/dess_mqtt.py
  - backend/config.py
  - tests/test_vrm_client.py
  - tests/test_dess_mqtt.py
autonomous: true
requirements: [DESS-01, DESS-02, DESS-04]

must_haves:
  truths:
    - "VrmClient polls VRM diagnostics endpoint with PAT auth header and caches results"
    - "DessMqttSubscriber connects to Venus OS MQTT and parses DESS schedule slots"
    - "Both clients degrade gracefully when credentials missing or connection fails"
    - "VrmConfig and DessConfig follow from_env() pattern with safe empty defaults"
  artifacts:
    - path: "backend/dess_models.py"
      provides: "DessScheduleSlot, DessSchedule, VrmDiagnostics dataclasses"
      contains: "class DessScheduleSlot"
    - path: "backend/vrm_client.py"
      provides: "VrmClient with async poll loop and cached diagnostics"
      exports: ["VrmClient"]
    - path: "backend/dess_mqtt.py"
      provides: "DessMqttSubscriber with paho MQTT Venus OS integration"
      exports: ["DessMqttSubscriber"]
    - path: "backend/config.py"
      provides: "VrmConfig and DessConfig dataclasses"
      contains: "class VrmConfig"
    - path: "tests/test_vrm_client.py"
      provides: "Unit tests for VRM client"
      contains: "test_"
    - path: "tests/test_dess_mqtt.py"
      provides: "Unit tests for DESS MQTT subscriber"
      contains: "test_"
  key_links:
    - from: "backend/vrm_client.py"
      to: "backend/dess_models.py"
      via: "imports VrmDiagnostics"
      pattern: "from backend.dess_models import VrmDiagnostics"
    - from: "backend/dess_mqtt.py"
      to: "backend/dess_models.py"
      via: "imports DessScheduleSlot, DessSchedule"
      pattern: "from backend.dess_models import"
    - from: "backend/vrm_client.py"
      to: "backend/config.py"
      via: "VrmConfig consumed by lifespan"
      pattern: "class VrmConfig"
---

<objective>
Create VRM REST client, DESS MQTT subscriber, data models, and config dataclasses for Phase 24 integration layer.

Purpose: Establishes the two new integration clients (VRM diagnostics + DESS schedule reading) that the coordinator will consume in Plan 02. Both clients follow established codebase patterns exactly.
Output: `dess_models.py`, `vrm_client.py`, `dess_mqtt.py`, extended `config.py`, unit tests for both clients.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-vrm-dess-integration/24-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From backend/config.py — config dataclass pattern:
```python
@dataclass
class HuaweiConfig:
    host: str
    port: int = 502
    # ...
    @classmethod
    def from_env(cls) -> "HuaweiConfig":
        return cls(
            host=_require_env("HUAWEI_HOST"),
            port=int(os.environ.get("HUAWEI_PORT", "502")),
        )
```

From backend/evcc_mqtt_driver.py — paho MQTT subscription pattern:
```python
class EvccMqttDriver:
    def __init__(self, host: str, port: int = 1883) -> None:
        self.evcc_available: bool = False
        self._loop: asyncio.AbstractEventLoop | None = None
        self._client: mqtt.Client = mqtt.Client(
            mqtt.CallbackAPIVersion.VERSION2,
            client_id="ems-evcc-driver",
        )
        self._client.on_connect = self._on_connect
        self._client.on_disconnect = self._on_disconnect
        self._client.on_message = self._on_message

    async def connect(self) -> None:
        self._loop = asyncio.get_event_loop()
        try:
            self._client.connect(self.host, self.port)
        except (ConnectionRefusedError, OSError) as exc:
            logger.warning("EVCC MQTT connect failed: %s", exc)
            return
        self._client.loop_start()
```

From backend/controller_model.py — CoordinatorState fields to add:
```python
@dataclass
class CoordinatorState:
    # ... existing fields ...
    commissioning_shadow_mode: bool = False
    # DESS fields will be added in Plan 02
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data models, config dataclasses, and VRM client</name>
  <files>backend/dess_models.py, backend/vrm_client.py, backend/config.py, tests/test_vrm_client.py</files>
  <read_first>
    - backend/config.py (full file — understand from_env pattern, _require_env helper, and where to add new configs)
    - backend/evcc_mqtt_driver.py (full file — reference async connect/availability pattern)
    - backend/controller_model.py (CoordinatorState — understand existing dataclass patterns)
    - .planning/phases/24-vrm-dess-integration/24-RESEARCH.md (VRM API details, VrmConfig/DessConfig code examples)
  </read_first>
  <behavior>
    - Test: VrmClient._fetch_diagnostics parses mock httpx response into VrmDiagnostics dataclass with correct field mapping
    - Test: VrmClient handles HTTP 429 rate limit by setting available=False and logging warning
    - Test: VrmClient handles connection error gracefully (available stays False, no exception raised)
    - Test: VrmClient marks diagnostics stale after 15 minutes (available becomes False)
    - Test: VrmConfig.from_env() reads VRM_TOKEN, VRM_SITE_ID, VRM_POLL_INTERVAL_S with empty defaults
    - Test: DessConfig.from_env() reads DESS_MQTT_HOST (falls back to VICTRON_HOST), DESS_MQTT_PORT, DESS_PORTAL_ID with empty defaults
  </behavior>
  <action>
    1. Create `backend/dess_models.py` with three dataclasses:
       - `DessScheduleSlot`: soc_pct (float, default 0.0), start_s (int, default 0), duration_s (int, default 0), strategy (int, default 0), active (bool, default False)
       - `DessSchedule`: slots (list of 4 DessScheduleSlot), mode (int, default 0), last_update (float, default 0.0)
       - `VrmDiagnostics`: battery_soc_pct (float|None), battery_power_w (float|None), grid_power_w (float|None), pv_power_w (float|None), consumption_w (float|None), timestamp (float, default 0.0)
       Follow `from __future__ import annotations`, type hints, docstrings per convention.

    2. Add `VrmConfig` and `DessConfig` to `backend/config.py`:
       - `VrmConfig`: token (str, default ""), site_id (str, default ""), poll_interval_s (float, default 300.0). `from_env()` reads VRM_TOKEN, VRM_SITE_ID, VRM_POLL_INTERVAL_S. All optional (empty = disabled).
       - `DessConfig`: host (str, default ""), port (int, default 1883), portal_id (str, default ""). `from_env()` reads DESS_MQTT_HOST (fallback VICTRON_HOST), DESS_MQTT_PORT, DESS_PORTAL_ID. All optional (empty = disabled).
       Place these after existing config classes, before any non-dataclass code.

    3. Create `backend/vrm_client.py`:
       - `VrmClient(token: str, site_id: int, poll_interval_s: float = 300.0)`
       - Properties: `available` (bool), `diagnostics` (VrmDiagnostics)
       - `async start()`: creates httpx.AsyncClient with base_url="https://vrmapi.victronenergy.com", header `X-Authorization: Token {token}`, timeout=30.0. Starts asyncio.create_task for _poll_loop.
       - `async stop()`: cancels task, closes httpx client.
       - `_poll_loop()`: while True, try _fetch_diagnostics except log warning + set available=False, await asyncio.sleep(poll_interval_s).
       - `_fetch_diagnostics()`: GET /v2/installations/{site_id}/diagnostics?count=100. Handle 429 (warning + available=False + return). Parse response JSON records into VrmDiagnostics. Set available=True. Check staleness: if diagnostics.timestamp is >900s old, set available=False.
       - `_parse_diagnostics(data: dict) -> VrmDiagnostics`: extract from records list by idDataAttribute. Log raw response at DEBUG on first successful parse. For unknown attributes, skip silently.

    4. Create `tests/test_vrm_client.py` with pytest + anyio tests. Mock httpx responses (use httpx's MockTransport or monkeypatch). Test happy path, 429, connection error, staleness detection, config from_env.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vrm_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class VrmDiagnostics" backend/dess_models.py
    - grep -q "class DessScheduleSlot" backend/dess_models.py
    - grep -q "class DessSchedule" backend/dess_models.py
    - grep -q "class VrmConfig" backend/config.py
    - grep -q "class DessConfig" backend/config.py
    - grep -q "class VrmClient" backend/vrm_client.py
    - grep -q "X-Authorization" backend/vrm_client.py
    - grep -q "available" backend/vrm_client.py
    - grep -q "def test_" tests/test_vrm_client.py
    - python -m pytest tests/test_vrm_client.py -x -q exits 0
  </acceptance_criteria>
  <done>VrmClient polls diagnostics with PAT auth, caches VrmDiagnostics, handles 429/errors/staleness gracefully. VrmConfig and DessConfig dataclasses follow from_env() pattern. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DESS MQTT subscriber</name>
  <files>backend/dess_mqtt.py, tests/test_dess_mqtt.py</files>
  <read_first>
    - backend/evcc_mqtt_driver.py (full file — mirror this pattern exactly for paho client, callbacks, thread-to-asyncio bridging)
    - backend/dess_models.py (just created in Task 1 — DessScheduleSlot, DessSchedule)
    - .planning/phases/24-vrm-dess-integration/24-RESEARCH.md (DESS MQTT topic paths, payload format, pitfalls 2/3/5/6)
  </read_first>
  <behavior>
    - Test: DessMqttSubscriber._on_message parses Schedule/0/Soc topic into slots[0].soc_pct
    - Test: DessMqttSubscriber._on_message parses Schedule/2/Start topic into slots[2].start_s
    - Test: DessMqttSubscriber._on_message parses Mode topic into schedule.mode
    - Test: DessMqttSubscriber ignores unknown subtopics without crashing
    - Test: DessMqttSubscriber handles connect failure gracefully (dess_available stays False)
    - Test: DessMqttSubscriber._on_disconnect sets dess_available to False
    - Test: DESS mode 0 means schedule is ignored (not treated as active)
  </behavior>
  <action>
    1. Create `backend/dess_mqtt.py` with `DessMqttSubscriber`:
       - Constructor: `(host: str, port: int = 1883, portal_id: str = "")`
       - Public attrs: `schedule` (DessSchedule), `dess_available` (bool, default False)
       - Internal: `_loop` (AbstractEventLoop|None), `_client` (mqtt.Client with CallbackAPIVersion.VERSION2, client_id="ems-dess-subscriber")
       - Wire callbacks: `_on_connect`, `_on_disconnect`, `_on_message`
       - `async connect()`: capture event loop, try self._client.connect(host, port) with same error handling as EvccMqttDriver (catch ConnectionRefusedError, OSError, log warning, return). Call loop_start().
       - `_on_connect`: subscribe to `N/{portal_id}/settings/0/Settings/DynamicEss/#`. Set dess_available=True via loop.call_soon_threadsafe.
       - `_on_disconnect`: set dess_available=False via loop.call_soon_threadsafe. Log warning if unexpected.
       - `_on_message`: parse topic path. Expected patterns:
         - `.../DynamicEss/Schedule/{slot_idx}/{field}` where slot_idx in 0-3, field in {Soc, Start, Duration, Strategy}
         - `.../DynamicEss/Mode`
         Payload is JSON `{"value": ...}`. Extract value, update self.schedule.slots[idx] or self.schedule.mode. Update self.schedule.last_update = time.time(). Use loop.call_soon_threadsafe for state mutations.
       - `def disconnect()`: call self._client.loop_stop() and self._client.disconnect(). Matches EvccMqttDriver pattern.
       - Helper: `get_active_slot(now_seconds_from_midnight: int) -> DessScheduleSlot | None`: returns the slot whose time window contains now, or None. Only returns if schedule.mode >= 1.

    2. Create `tests/test_dess_mqtt.py`:
       - Test message parsing for each DESS topic field (Soc, Start, Duration, Strategy, Mode).
       - Test unknown topic handling (no crash).
       - Test connect failure graceful degradation.
       - Test get_active_slot with mode=0 returns None.
       - Test get_active_slot with overlapping time window.
       - Mock paho client (monkeypatch connect to avoid real network).
  </action>
  <verify>
    <automated>python -m pytest tests/test_dess_mqtt.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class DessMqttSubscriber" backend/dess_mqtt.py
    - grep -q "ems-dess-subscriber" backend/dess_mqtt.py
    - grep -q "DynamicEss" backend/dess_mqtt.py
    - grep -q "dess_available" backend/dess_mqtt.py
    - grep -q "get_active_slot" backend/dess_mqtt.py
    - grep -q "def test_" tests/test_dess_mqtt.py
    - python -m pytest tests/test_dess_mqtt.py -x -q exits 0
  </acceptance_criteria>
  <done>DessMqttSubscriber connects to Venus OS MQTT, subscribes to DynamicEss topics, parses schedule slots and mode, provides get_active_slot() for coordinator consumption. Graceful degradation on connect failure. All tests pass.</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_vrm_client.py tests/test_dess_mqtt.py -q
</verification>

<success_criteria>
- VrmClient exists with async poll loop, PAT auth, cached diagnostics, staleness detection
- DessMqttSubscriber exists with paho MQTT connection, DESS topic parsing, get_active_slot()
- VrmConfig and DessConfig in config.py with from_env() and safe empty defaults
- DessScheduleSlot, DessSchedule, VrmDiagnostics models in dess_models.py
- All unit tests pass
- No new pip dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/24-vrm-dess-integration/24-01-SUMMARY.md`
</output>
