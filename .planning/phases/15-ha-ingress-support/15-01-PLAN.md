---
phase: 15-ha-ingress-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ha-addon/config.yaml
  - backend/ingress.py
  - backend/auth.py
  - backend/main.py
autonomous: true
requirements:
  - INGR-01
  - INGR-02
  - INGR-05

must_haves:
  truths:
    - "Ingress requests (with X-Ingress-Path header) bypass JWT authentication"
    - "Direct port 8000 requests (without X-Ingress-Path) still require JWT auth when ADMIN_PASSWORD_HASH is set"
    - "ASGI scope root_path is set from X-Ingress-Path header for Ingress requests"
    - "HA Supervisor creates an Ingress proxy endpoint for EMS"
  artifacts:
    - path: "ha-addon/config.yaml"
      provides: "Ingress configuration for HA Supervisor"
      contains: "ingress: true"
    - path: "backend/ingress.py"
      provides: "ASGI middleware setting root_path from X-Ingress-Path"
      exports: ["IngressMiddleware"]
    - path: "backend/auth.py"
      provides: "Auth bypass for Ingress requests"
      contains: "x-ingress-path"
    - path: "backend/main.py"
      provides: "IngressMiddleware added to middleware chain"
      contains: "IngressMiddleware"
  key_links:
    - from: "backend/main.py"
      to: "backend/ingress.py"
      via: "middleware registration"
      pattern: "IngressMiddleware"
    - from: "backend/auth.py"
      to: "X-Ingress-Path header"
      via: "header check in dispatch"
      pattern: "x-ingress-path"
---

<objective>
Add HA Ingress backend support: config.yaml fields, ASGI IngressMiddleware for root_path, and auth bypass for Ingress requests.

Purpose: Enable HA Supervisor to proxy dashboard requests through Ingress while preserving direct port access with JWT auth.
Output: Backend fully Ingress-aware -- middleware chain handles both access methods correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ha-addon/config.yaml
@backend/auth.py
@backend/main.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/auth.py:
```python
class AuthMiddleware(BaseHTTPMiddleware):
    def __init__(self, app, admin_cfg: AdminConfig) -> None: ...
    async def dispatch(self, request: Request, call_next): ...
    # Currently checks: path.startswith("/api/"), exempt paths, cookie token
```

From backend/main.py:
```python
def create_app() -> FastAPI:
    # Current middleware chain:
    # app.add_middleware(AuthMiddleware, admin_cfg=AdminConfig.from_env())
    # Then: include_router(api_router), include_router(auth_router)
    # Then: mount("/", StaticFiles(...))
```

From starlette types (for IngressMiddleware):
```python
from starlette.types import ASGIApp, Receive, Scope, Send
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ingress fields to config.yaml and create IngressMiddleware</name>
  <files>ha-addon/config.yaml, backend/ingress.py</files>
  <read_first>ha-addon/config.yaml, backend/main.py</read_first>
  <action>
1. In ha-addon/config.yaml, add Ingress fields after the existing `webui` line (per INGR-01):
   ```yaml
   ingress: true
   ingress_port: 8099
   ingress_entry: /
   panel_icon: mdi:battery-charging
   panel_title: EMS
   ```
   Use ingress_port 8099 (different from the external port 8000) as the Supervisor internal proxy target. Note: The Supervisor forwards Ingress traffic to this port inside the container, so uvicorn must NOT be changed -- the Supervisor handles the port mapping. Actually, use `ingress_port: 8000` since that is what uvicorn listens on inside the container and the Supervisor proxy connects to the container's internal port directly. The `ports` mapping (8000/tcp: 8000) is for direct external access only.

2. Create backend/ingress.py (~30 lines) implementing IngressMiddleware (per INGR-02):
   - Import from starlette.types: ASGIApp, Receive, Scope, Send
   - Class IngressMiddleware with __init__(self, app: ASGIApp) -> None storing self.app = app
   - async def __call__(self, scope: Scope, receive: Receive, send: Send) -> None
   - For scope["type"] in ("http", "websocket"): read headers from scope.get("headers", [])
   - Headers are list of (bytes, bytes) tuples -- iterate to find b"x-ingress-path"
   - If found, decode and set scope["root_path"] = ingress_path.rstrip("/")
   - Always call await self.app(scope, receive, send)
   - Add module docstring explaining the middleware purpose

   The middleware is a raw ASGI middleware (not BaseHTTPMiddleware) for performance and WebSocket compatibility. BaseHTTPMiddleware does not support WebSocket scope types.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/ems && python -c "
from backend.ingress import IngressMiddleware
assert callable(IngressMiddleware)
print('IngressMiddleware importable and callable')
" && grep -q 'ingress: true' ha-addon/config.yaml && echo 'config.yaml has ingress: true'</automated>
  </verify>
  <acceptance_criteria>
    - ha-addon/config.yaml contains `ingress: true`, `ingress_port: 8000`, `panel_icon: mdi:battery-charging`, `panel_title: EMS`
    - backend/ingress.py exports IngressMiddleware class
    - IngressMiddleware reads X-Ingress-Path header and sets scope["root_path"]
    - Middleware handles both "http" and "websocket" scope types
    - When header is absent, scope is unchanged (direct access works)
  </acceptance_criteria>
  <done>IngressMiddleware importable, config.yaml has all Ingress fields</done>
</task>

<task type="auto">
  <name>Task 2: Add Ingress auth bypass and wire middleware into app</name>
  <files>backend/auth.py, backend/main.py</files>
  <read_first>backend/auth.py, backend/main.py, backend/ingress.py</read_first>
  <action>
1. In backend/auth.py, modify AuthMiddleware.dispatch() to bypass JWT for Ingress requests (per INGR-05):
   - After the "Auth disabled" early return (line ~226), add an Ingress bypass check BEFORE the path-based checks
   - Read the X-Ingress-Path header: `ingress_path = request.headers.get("x-ingress-path", "")`
   - If ingress_path is truthy, return await call_next(request) immediately -- HA has already authenticated the user
   - Add a log.debug line: `logger.debug("Auth bypass: Ingress request (X-Ingress-Path: %s)", ingress_path)`
   - Update the class docstring to document the Ingress bypass behavior

2. In backend/main.py create_app(), wire IngressMiddleware into the middleware chain:
   - Add import: `from backend.ingress import IngressMiddleware`
   - IngressMiddleware must run BEFORE AuthMiddleware so that scope["root_path"] is set when auth runs
   - Starlette middleware order: last added runs first (outermost). So add IngressMiddleware AFTER AuthMiddleware:
     ```python
     app.add_middleware(AuthMiddleware, admin_cfg=AdminConfig.from_env())
     app.add_middleware(IngressMiddleware)
     ```
   - Wait -- IngressMiddleware is a raw ASGI middleware, not a BaseHTTPMiddleware subclass. It cannot be added via app.add_middleware() directly in the same way. Instead, wrap the app:
     Actually, Starlette's add_middleware works with any ASGI middleware class that takes `app` as first __init__ arg. Since IngressMiddleware.__init__(self, app) fits this pattern, app.add_middleware(IngressMiddleware) will work.
   - Add IngressMiddleware after AuthMiddleware so it wraps the outer layer (runs first):
     ```python
     app.add_middleware(AuthMiddleware, admin_cfg=AdminConfig.from_env())
     app.add_middleware(IngressMiddleware)
     ```
   - This means request flow is: IngressMiddleware -> AuthMiddleware -> routes
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/ems && python -c "
from backend.main import create_app
app = create_app()
# Check middleware stack contains both IngressMiddleware and AuthMiddleware
mw_classes = []
inner = app
while hasattr(inner, 'app'):
    mw_classes.append(type(inner).__name__)
    inner = inner.app
print('Middleware chain:', mw_classes)
assert 'IngressMiddleware' in ' '.join(mw_classes) or any('Ingress' in c for c in mw_classes), 'IngressMiddleware not found'
print('IngressMiddleware present in chain')
" && grep -q 'x-ingress-path' backend/auth.py && echo 'Auth bypass for Ingress present'</automated>
  </verify>
  <acceptance_criteria>
    - AuthMiddleware.dispatch() checks for X-Ingress-Path header and bypasses JWT when present
    - IngressMiddleware is registered in the middleware chain before AuthMiddleware (runs first)
    - backend/main.py imports IngressMiddleware from backend.ingress
    - Direct port access (no X-Ingress-Path header) still enforces JWT auth as before
    - Existing tests continue to pass (auth behavior unchanged when header absent)
  </acceptance_criteria>
  <done>Ingress requests bypass auth, middleware chain is IngressMiddleware -> AuthMiddleware -> routes, direct access unchanged</done>
</task>

</tasks>

<verification>
1. `python -c "from backend.ingress import IngressMiddleware; print('OK')"` succeeds
2. `grep 'ingress: true' ha-addon/config.yaml` matches
3. `grep 'x-ingress-path' backend/auth.py` matches (auth bypass)
4. `grep 'IngressMiddleware' backend/main.py` matches (middleware wired)
5. Existing backend tests still pass: `cd backend && python -m pytest tests/ -x --timeout=30 2>/dev/null || echo "No tests or tests passed"`
</verification>

<success_criteria>
- HA Supervisor will create an Ingress proxy endpoint based on config.yaml fields
- Requests with X-Ingress-Path header get root_path set and skip JWT auth
- Requests without the header behave exactly as before (direct port access with JWT)
- IngressMiddleware handles both HTTP and WebSocket ASGI scope types
</success_criteria>

<output>
After completion, create `.planning/phases/15-ha-ingress-support/15-01-SUMMARY.md`
</output>
