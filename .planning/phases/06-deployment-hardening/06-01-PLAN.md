---
phase: 06-deployment-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker-compose.yml
  - ha-addon/config.yaml
  - ha-addon/run.sh
  - ha-addon/translations/en.yaml
  - ha-addon/translations/de.yaml
  - ha-addon/Dockerfile
  - ha-addon/backend/
  - ha-addon/pyproject.toml
  - ha-addon/dist/
autonomous: true
requirements: [DEP-01, DEP-02]

must_haves:
  truths:
    - "A single Dockerfile at repo root builds a working image for both HA Add-on (aarch64/amd64) and local dev"
    - "No stale duplicate source code exists in ha-addon/"
    - "config.yaml schema includes Victron Modbus unit IDs, coordinator tuning, and Modul3 tariff fields"
    - "run.sh exports all new env vars from options.json to the backend"
    - "Service discovery for MQTT, EVCC, InfluxDB works via existing SupervisorClient"
  artifacts:
    - path: "Dockerfile"
      provides: "Consolidated multi-stage Dockerfile (Node.js frontend build + HA base Python runtime)"
      contains: "FROM node:20-alpine AS frontend-build"
    - path: "ha-addon/config.yaml"
      provides: "HA Add-on schema with all new fields"
      contains: "victron_system_unit_id"
    - path: "ha-addon/run.sh"
      provides: "Options.json to env var bridge with new Victron, coordinator, and Modul3 exports"
      contains: "VICTRON_SYSTEM_UNIT_ID"
  key_links:
    - from: "ha-addon/config.yaml"
      to: "ha-addon/run.sh"
      via: "option names match between schema and get_option calls"
      pattern: "get_option.*victron_system_unit_id"
    - from: "ha-addon/run.sh"
      to: "backend/config.py VictronConfig.from_env()"
      via: "env var names match"
      pattern: "VICTRON_SYSTEM_UNIT_ID"
    - from: "Dockerfile"
      to: "ha-addon/build.yaml"
      via: "ARG BUILD_FROM default matches build.yaml amd64 value"
      pattern: "BUILD_FROM.*ghcr.io/home-assistant"
---

<objective>
Consolidate the Dockerfile, clean up stale ha-addon/ duplicates, and extend config.yaml + run.sh for the v2 dual-controller architecture.

Purpose: Eliminate the stale ha-addon/backend/ copy (27 files), ha-addon/pyproject.toml, ha-addon/dist/ (4 files), and ha-addon/Dockerfile. Replace with a single multi-stage Dockerfile at repo root that builds frontend from source and uses HA base images. Extend config.yaml schema and run.sh for Victron Modbus unit IDs, coordinator tuning, and Modul3 tariff fields.

Output: Consolidated Dockerfile, cleaned ha-addon/ directory, updated config.yaml + run.sh + translations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment-hardening/06-CONTEXT.md
@.planning/phases/06-deployment-hardening/06-RESEARCH.md

<interfaces>
<!-- Existing VictronConfig.from_env() env var names (source of truth for run.sh export names) -->
From backend/config.py:
```python
class VictronConfig:
    host: str
    port: int = 502
    timeout_s: float = 5.0
    vebus_unit_id: int = 227
    system_unit_id: int = 100

    @classmethod
    def from_env(cls) -> "VictronConfig":
        return cls(
            host=_require_env("VICTRON_HOST"),
            port=int(os.environ.get("VICTRON_PORT", "502")),
            vebus_unit_id=int(os.environ.get("VICTRON_VEBUS_UNIT_ID", "227")),
            system_unit_id=int(os.environ.get("VICTRON_SYSTEM_UNIT_ID", "100")),
        )
```

From ha-addon/build.yaml:
```yaml
build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base-python:3.12-alpine3.21
  amd64: ghcr.io/home-assistant/amd64-base-python:3.12-alpine3.21
```

From ha-addon/run.sh (existing pattern for optional exports):
```bash
get_option() {
    jq -r --arg key "$1" '.[$key] // ""' /data/options.json
}
_huawei_host=$(get_option 'huawei_host')
[ -n "$_huawei_host" ] && export HUAWEI_HOST="$_huawei_host"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Dockerfile and delete stale ha-addon duplicates</name>
  <files>Dockerfile, ha-addon/Dockerfile, ha-addon/backend/, ha-addon/pyproject.toml, ha-addon/dist/, docker-compose.yml</files>
  <read_first>Dockerfile, ha-addon/Dockerfile, ha-addon/build.yaml, docker-compose.yml, ha-addon/run.sh</read_first>
  <action>
1. **Replace root `Dockerfile`** with a consolidated multi-stage build (per D-01, D-02, D-03):

```dockerfile
# Stage 1: Frontend build
FROM node:20-alpine AS frontend-build
WORKDIR /app/frontend
COPY frontend/package*.json ./
RUN npm ci
COPY frontend/ ./
RUN npm run build

# Stage 2: Python runtime (HA Add-on base)
ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base-python:3.12-alpine3.21
FROM ${BUILD_FROM}

ARG BUILD_ARCH
ARG BUILD_VERSION

WORKDIR /app

# System deps: build-base + libffi-dev for bcrypt, gfortran + openblas-dev for sklearn, jq for run.sh
RUN apk add --no-cache build-base libffi-dev gfortran openblas-dev jq

# Python dependencies
COPY pyproject.toml .
RUN pip install --no-cache-dir .

# Application source
COPY backend/ backend/
COPY --from=frontend-build /app/frontend/dist frontend/dist

# Entrypoint
COPY ha-addon/run.sh /run.sh
RUN chmod +x /run.sh

LABEL \
    io.hass.name="EMS" \
    io.hass.description="Energy Management System for Huawei + Victron" \
    io.hass.arch="${BUILD_ARCH}" \
    io.hass.type="addon" \
    io.hass.version="${BUILD_VERSION}"

EXPOSE 8000
ENTRYPOINT ["/run.sh"]
```

2. **Delete stale files** (per D-01, D-04):
   - `rm -rf ha-addon/backend/` (entire directory, ~27 files)
   - `rm ha-addon/pyproject.toml`
   - `rm -rf ha-addon/dist/` (entire directory, ~4 files)
   - `rm ha-addon/Dockerfile` (replaced by root Dockerfile)

3. **Update `docker-compose.yml`** to use the consolidated Dockerfile:
   - Change `build: .` to explicit `build: { context: ., dockerfile: Dockerfile }`
   - The HA Alpine base image works fine for dev; no override needed
   - Add `VICTRON_PORT: ${VICTRON_PORT:-502}` to environment section (port default was 1883, now 502)

The `build.yaml` `build_from` entries remain unchanged -- the HA Add-on builder passes `BUILD_FROM` as a build arg automatically.
  </action>
  <verify>
    <automated>test ! -d ha-addon/backend && test ! -f ha-addon/pyproject.toml && test ! -d ha-addon/dist && test ! -f ha-addon/Dockerfile && grep -q "frontend-build" Dockerfile && grep -q "BUILD_FROM" Dockerfile && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `ha-addon/backend/` directory does not exist
    - `ha-addon/pyproject.toml` file does not exist
    - `ha-addon/dist/` directory does not exist
    - `ha-addon/Dockerfile` file does not exist
    - Root `Dockerfile` contains `FROM node:20-alpine AS frontend-build`
    - Root `Dockerfile` contains `ARG BUILD_FROM=ghcr.io/home-assistant/amd64-base-python:3.12-alpine3.21`
    - Root `Dockerfile` contains `COPY --from=frontend-build /app/frontend/dist frontend/dist`
    - Root `Dockerfile` contains `COPY ha-addon/run.sh /run.sh`
    - Root `Dockerfile` contains `ENTRYPOINT ["/run.sh"]`
    - `docker-compose.yml` ems service build context points to root Dockerfile
  </acceptance_criteria>
  <done>Single consolidated multi-stage Dockerfile at repo root; all stale ha-addon duplicates removed; docker-compose.yml updated</done>
</task>

<task type="auto">
  <name>Task 2: Extend config.yaml schema, run.sh exports, and translations for v2 fields</name>
  <files>ha-addon/config.yaml, ha-addon/run.sh, ha-addon/translations/en.yaml, ha-addon/translations/de.yaml</files>
  <read_first>ha-addon/config.yaml, ha-addon/run.sh, ha-addon/translations/en.yaml, ha-addon/translations/de.yaml, backend/config.py</read_first>
  <action>
1. **Update `ha-addon/config.yaml`** (per D-09, D-10, D-11, D-13):

   In `options:` section:
   - Change `victron_port: 1883` to `victron_port: 502` (per D-10)
   - Add after victron_port:
     ```yaml
     victron_system_unit_id: 100
     victron_battery_unit_id: 225
     victron_vebus_unit_id: 227
     ```
   - Add coordinator tuning section (after InfluxDB block):
     ```yaml
     # --- Coordinator tuning (advanced) ---
     huawei_deadband_w: 400
     victron_deadband_w: 150
     ramp_rate_w_per_cycle: 500
     min_soc_pct_huawei: 10
     min_soc_pct_victron: 15
     ```
   - Add Modul3 tariff section (after log_level):
     ```yaml
     # --- Modul3 grid-fee tariff ---
     modul3_surplus_start_min: ""
     modul3_surplus_end_min: ""
     modul3_deficit_start_min: ""
     modul3_deficit_end_min: ""
     modul3_surplus_rate_eur_kwh: ""
     modul3_deficit_rate_eur_kwh: ""
     ```

   In `schema:` section:
   - Change `victron_port: int` (already correct type, just verify)
   - Add after `victron_port`:
     ```yaml
     victron_system_unit_id: int?
     victron_battery_unit_id: int?
     victron_vebus_unit_id: int?
     ```
   - Add coordinator tuning:
     ```yaml
     # --- Coordinator tuning (advanced) ---
     huawei_deadband_w: int?
     victron_deadband_w: int?
     ramp_rate_w_per_cycle: int?
     min_soc_pct_huawei: int?
     min_soc_pct_victron: int?
     ```
   - Add Modul3:
     ```yaml
     # --- Modul3 grid-fee tariff ---
     modul3_surplus_start_min: int?
     modul3_surplus_end_min: int?
     modul3_deficit_start_min: int?
     modul3_deficit_end_min: int?
     modul3_surplus_rate_eur_kwh: str?
     modul3_deficit_rate_eur_kwh: str?
     ```

2. **Update `ha-addon/run.sh`** to export new env vars:

   After the existing `export VICTRON_PORT=...` line, add (using existing env var names from VictronConfig.from_env()):
   ```bash
   # --- Victron Modbus unit IDs (optional, defaults in VictronConfig) ---
   _victron_sys_unit=$(get_option 'victron_system_unit_id')
   [ -n "$_victron_sys_unit" ] && export VICTRON_SYSTEM_UNIT_ID="$_victron_sys_unit"
   _victron_bat_unit=$(get_option 'victron_battery_unit_id')
   [ -n "$_victron_bat_unit" ] && export VICTRON_BATTERY_UNIT_ID="$_victron_bat_unit"
   _victron_vb_unit=$(get_option 'victron_vebus_unit_id')
   [ -n "$_victron_vb_unit" ] && export VICTRON_VEBUS_UNIT_ID="$_victron_vb_unit"
   ```

   After the logging section, add:
   ```bash
   # --- Coordinator tuning (advanced, optional) ---
   _hw_db=$(get_option 'huawei_deadband_w')
   [ -n "$_hw_db" ] && export HUAWEI_DEADBAND_W="$_hw_db"
   _vic_db=$(get_option 'victron_deadband_w')
   [ -n "$_vic_db" ] && export VICTRON_DEADBAND_W="$_vic_db"
   _ramp=$(get_option 'ramp_rate_w_per_cycle')
   [ -n "$_ramp" ] && export RAMP_RATE_W_PER_CYCLE="$_ramp"
   _min_hw=$(get_option 'min_soc_pct_huawei')
   [ -n "$_min_hw" ] && export MIN_SOC_PCT_HUAWEI="$_min_hw"
   _min_vic=$(get_option 'min_soc_pct_victron')
   [ -n "$_min_vic" ] && export MIN_SOC_PCT_VICTRON="$_min_vic"

   # --- Modul3 grid-fee tariff (optional) ---
   _m3_ss=$(get_option 'modul3_surplus_start_min')
   [ -n "$_m3_ss" ] && export MODUL3_SURPLUS_START_MIN="$_m3_ss"
   _m3_se=$(get_option 'modul3_surplus_end_min')
   [ -n "$_m3_se" ] && export MODUL3_SURPLUS_END_MIN="$_m3_se"
   _m3_ds=$(get_option 'modul3_deficit_start_min')
   [ -n "$_m3_ds" ] && export MODUL3_DEFICIT_START_MIN="$_m3_ds"
   _m3_de=$(get_option 'modul3_deficit_end_min')
   [ -n "$_m3_de" ] && export MODUL3_DEFICIT_END_MIN="$_m3_de"
   _m3_sr=$(get_option 'modul3_surplus_rate_eur_kwh')
   [ -n "$_m3_sr" ] && export MODUL3_SURPLUS_RATE_EUR_KWH="$_m3_sr"
   _m3_dr=$(get_option 'modul3_deficit_rate_eur_kwh')
   [ -n "$_m3_dr" ] && export MODUL3_DEFICIT_RATE_EUR_KWH="$_m3_dr"
   ```

3. **Update `ha-addon/translations/en.yaml`**:

   Change existing Victron entries:
   - `victron_host.name`: "Victron Modbus TCP Host"
   - `victron_host.description`: "IP address of the Venus OS GX device (Modbus TCP server). Default port is 502."
   - `victron_port.name`: "Victron Modbus TCP Port"
   - `victron_port.description`: "Modbus TCP port on the Venus OS GX device. Default is 502."

   Add new entries under Victron section:
   ```yaml
   victron_system_unit_id:
     name: Victron System Unit ID
     description: >-
       Modbus unit ID for system-level registers (SoC, total power).
       Default is 100 — only change if your Venus OS uses non-standard addressing.
   victron_battery_unit_id:
     name: Victron Battery Unit ID
     description: >-
       Modbus unit ID for battery monitor registers. Default is 225.
       Reserved for future use — most installations use the default.
   victron_vebus_unit_id:
     name: Victron VE.Bus Unit ID
     description: >-
       Modbus unit ID for the VE.Bus inverter (ESS setpoint writes).
       Default is 227 — only change if your Venus OS uses non-standard addressing.
   ```

   Add coordinator tuning entries:
   ```yaml
   huawei_deadband_w:
     name: Huawei Deadband (W)
     description: Minimum power change threshold for Huawei setpoints. Default 400 W.
   victron_deadband_w:
     name: Victron Deadband (W)
     description: Minimum power change threshold for Victron setpoints. Default 150 W.
   ramp_rate_w_per_cycle:
     name: Ramp Rate (W/cycle)
     description: Maximum setpoint change per control cycle. Default 500 W.
   min_soc_pct_huawei:
     name: Huawei Min SoC (%)
     description: Minimum state-of-charge for Huawei battery. Default 10%.
   min_soc_pct_victron:
     name: Victron Min SoC (%)
     description: Minimum state-of-charge for Victron battery. Default 15%.
   ```

   Add Modul3 entries:
   ```yaml
   modul3_surplus_start_min:
     name: Modul3 Surplus Start (min)
     description: Start of surplus grid-fee window in minutes from midnight.
   modul3_surplus_end_min:
     name: Modul3 Surplus End (min)
     description: End of surplus grid-fee window in minutes from midnight.
   modul3_deficit_start_min:
     name: Modul3 Deficit Start (min)
     description: Start of deficit grid-fee window in minutes from midnight.
   modul3_deficit_end_min:
     name: Modul3 Deficit End (min)
     description: End of deficit grid-fee window in minutes from midnight.
   modul3_surplus_rate_eur_kwh:
     name: Modul3 Surplus Rate
     description: Grid-fee rate during surplus window in EUR/kWh.
   modul3_deficit_rate_eur_kwh:
     name: Modul3 Deficit Rate
     description: Grid-fee rate during deficit window in EUR/kWh.
   ```

4. **Update `ha-addon/translations/de.yaml`** with German translations following the same structure. Use German labels (e.g., "Victron Modbus TCP Host", "Modbus-Einheiten-ID", "Netzentgelt", etc.).
  </action>
  <verify>
    <automated>grep -q "victron_system_unit_id" ha-addon/config.yaml && grep -q "victron_port: 502" ha-addon/config.yaml && grep -q "VICTRON_SYSTEM_UNIT_ID" ha-addon/run.sh && grep -q "MODUL3_SURPLUS_START_MIN" ha-addon/run.sh && grep -q "victron_system_unit_id" ha-addon/translations/en.yaml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `ha-addon/config.yaml` options section contains `victron_port: 502` (not 1883)
    - `ha-addon/config.yaml` options section contains `victron_system_unit_id: 100`
    - `ha-addon/config.yaml` options section contains `victron_battery_unit_id: 225`
    - `ha-addon/config.yaml` options section contains `victron_vebus_unit_id: 227`
    - `ha-addon/config.yaml` schema section contains `victron_system_unit_id: int?`
    - `ha-addon/config.yaml` schema section contains `huawei_deadband_w: int?`
    - `ha-addon/config.yaml` schema section contains `modul3_surplus_start_min: int?`
    - `ha-addon/run.sh` contains `export VICTRON_SYSTEM_UNIT_ID=`
    - `ha-addon/run.sh` contains `export VICTRON_BATTERY_UNIT_ID=`
    - `ha-addon/run.sh` contains `export VICTRON_VEBUS_UNIT_ID=`
    - `ha-addon/run.sh` contains `export MODUL3_SURPLUS_START_MIN=`
    - `ha-addon/run.sh` contains `export HUAWEI_DEADBAND_W=`
    - `ha-addon/translations/en.yaml` contains `victron_system_unit_id:` entry with name and description
    - `ha-addon/translations/en.yaml` contains `modul3_surplus_start_min:` entry
    - `ha-addon/translations/de.yaml` contains `victron_system_unit_id:` entry
  </acceptance_criteria>
  <done>config.yaml schema has all v2 fields; run.sh exports all new env vars using the nonempty-only pattern; en.yaml and de.yaml have labels and descriptions for every new field</done>
</task>

</tasks>

<verification>
- `test ! -d ha-addon/backend && test ! -f ha-addon/Dockerfile` confirms stale files removed
- `grep -c "victron_system_unit_id" ha-addon/config.yaml` returns 2 (options + schema)
- `grep -c "VICTRON_SYSTEM_UNIT_ID" ha-addon/run.sh` returns 1
- `grep "BUILD_FROM" Dockerfile` confirms multi-stage build accepts arch argument
</verification>

<success_criteria>
- Single Dockerfile at repo root with multi-stage Node.js + HA base build
- ha-addon/ contains only: config.yaml, build.yaml, run.sh, translations/, DOCS.md, CHANGELOG.md, icon.png, logo.png, .dockerignore
- config.yaml has 3 Victron unit ID fields, 5 coordinator tuning fields, 6 Modul3 tariff fields in both options and schema
- run.sh exports all new fields using the existing nonempty-only pattern
- Both en.yaml and de.yaml have translations for all new fields
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-hardening/06-01-SUMMARY.md`
</output>
